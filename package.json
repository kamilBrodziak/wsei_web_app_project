{
  "name": "web_app_wsei_project",
  "version": "0.0.1",
  "description": "",
  "keywords": ["web", "app", "wsei"],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
  }
}
